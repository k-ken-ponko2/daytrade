# 株式デイトレ自動売買 構成ドキュメント

最終整理日：2026-06-08

> 本ドキュメントは、過去のセッションで決定した内容を再構成してまとめたもの。
> 個人の検討メモであり投資助言ではない。最終判断は自己責任で行う。

---

## 0. 目標と前提

- **元手**：30万円
- **目標**：年間利益100万円（残高130万円 ＝ 約4.3倍）
- **対象市場**：日本株
- **手法**：デイトレード／スキャルピングの自動売買
- **認識**：4.3倍は再現性のある手法では極めて困難。達成より先に「退場しないこと」を最優先に設計する。

---

## 1. 全体構成（役割分担）※確定

| 役割 | 使うもの（確定） | 補足 |
|---|---|---|
| 発注・約定・リアルタイムデータ | **kabuステーションAPI** | 個人向け公式株APIは実質これ一択。板・歩み値も取得可 |
| 過去データ（検証用） | **J-Quants API：Standard プラン** | 10年分。デイトレ検証なら分足アドオンを追加 |
| 開発言語 | **Python** | 日本語の作例・記事が豊富 |
| バックテストエンジン | **Backtrader ＋ NautilusTrader（並行運用）** | 同一ロジックを2エンジンで二重検証 |
| 指標計算 | pandas-ta（手軽）／ TA-Lib（高速・網羅） | エントリー変数の計算 |
| データ処理 | pandas / numpy / matplotlib（または plotly） | 必須の土台 |
| 稼働環境（検証） | 自宅PC（Windows） | 追加費用0円 |
| 稼働環境（本番） | **国内Windows VPS** | AWS/GCPは不採用。単純さとコストで国内VPSに確定 |
| 口座種別 | 特定口座（源泉徴収あり） | 税の天引きで確定申告が楽 |

**重要な役割分担**：実運用中のリアルタイム判定は kabuステーションAPI（板・歩み値）で行う。J-Quants は「過去検証専用」と割り切る。

---

## 2. 証券会社・発注API：kabuステーションAPI（三菱UFJ eスマート証券）

- 個人向けに公式RESTを提供している実質唯一の選択肢。
- REST／WebSocket対応。売買注文・注文状況・保有銘柄・板・歩み値の取得が可能。
- **API利用料・口座維持費・通常プラン料金：すべて無料**。
- 利用には **Professionalプラン（無料）** が必要。
  - 維持条件：信用取引口座を開設し、一定期間に最低1回取引すれば達成（＝下限条件）。
  - 初回利用時は約2ヶ月間無条件で適用される。
  - デイトレで売買していれば自動的に維持される。
- **制約**：kabuステーションアプリ（Windows専用）を起動した状態でないとAPIが動作しない。

> プランは Professional で確定。上位の Premium はAPI利用には不要。

---

## 3. 過去検証データ：J-Quants API

### プランと料金（月額・税込）

| プラン | 月額 | データ範囲 | 用途 |
|---|---|---|---|
| Free | 0円 | 12週間遅延 | 接続確認・コードの動作テスト |
| Light | 1,650円 | 約2年 | 中途半端なので不採用 |
| Standard | 3,300円 | 10年分 | バックテストの実用最低ライン（採用） |
| Premium | 16,500円 | 全範囲（約15年） | 本格・長期検証 |
| 分足・Tickアドオン | +5,500円 | — | デイトレ検証には実質必須 |

### 採用方針 ※確定

- 採用：**Standard（3,300円）＋ 分足アドオン（5,500円）＝ 月8,800円**。
- 常時契約しない。**検証作業をする数ヶ月だけ契約 → 終わったら解約**。
- まず Free（0円）でデータ取得コードの動作確認を済ませ、骨格ができてから有償契約して集中検証する。

---

## 4. コスト構造（取引回数との関係）

- 手数料以外のランニングコスト（VPS／クラウド・J-Quants API・kabuステーションAPI）は**すべて固定費**。取引回数では変わらない。
- 唯一かろうじて回数が関わるのは kabuステーション Professionalプランの「最低1回」という下限条件のみ（デイトレなら自動で満たす）。
- **取引回数に直結するのは証券会社の売買手数料のみ**。高回転戦略では、この手数料とスリッページが利益を最も食う要素になる。

---

## 5. バックテストエンジン方針（Backtrader ＋ NautilusTrader 並行運用）

### 狙い

同一の売買ロジックを2つのエンジンで実装し、結果を突き合わせて二重検証する。

- **Backtrader**：日本語情報・カスタマイズ例が豊富で学習効率が高い。ただし更新は停滞気味（枯れている）。
- **NautilusTrader**：執行メカニクス（注文種別・遅延・板の深さ）に強く、バックテスト→実運用への移行に有利。保守も活発。学習コストはやや高い。

### 設計指針（重要）

1. **ロジックの「単一の真実」を持つ**：売買判定の中核ロジックをエンジン非依存の純Python関数として切り出し、Backtrader と NautilusTrader はそれを呼ぶだけにする。ロジック修正は1箇所で済み、両エンジンの比較が「エンジンの差」だけに絞れる。
2. **結果が完全一致はしない前提**：約定価格・手数料計算・端数処理がエンジンごとに微妙に違う。「ほぼ一致」を目標とし、許容できるズレの範囲を最初に決めておく。
3. **工数はアダプタ層だけに抑える**：中核ロジックを共通化しておけば、増えるのは各エンジンへの接続部分（アダプタ層）だけ。

```
共通ロジック層（エンジン非依存・純Python）
  ├─ 指標計算（pandas-ta / TA-Lib）
  └─ 売買判定関数（シグナル生成）  ← 単一の真実
        ↓ 同じ関数を両方が呼ぶ
  ┌────────────┴────────────┐
Backtraderアダプタ      NautilusTraderアダプタ
  └─ 結果を共通フォーマットで出力 → 突き合わせ比較
```

---

## 6. 売買ロジックで参照する変数（デイトレ）

### エントリー判定

- 価格と移動平均の位置関係（短期・長期のクロス）
- 出来高の急増（平常時比）
- 板の厚み・気配の偏り
- VWAP（出来高加重平均）からの乖離
- 当日高値／安値の更新
- ボラティリティ（ATR）の水準
- 寄り付きからの値動きパターン

複数を組み合わせて「条件A かつ B かつ C ならエントリー」と定義する。

### 決済判定

- 利確幅（ATRの倍数）
- 損切り幅
- 保有時間の上限（例：◯分で手仕舞い）
- トレーリングストップ
- 引け前の強制クローズ（持ち越さない）

---

## 7. リスク管理ルール（先に決める）

- **1トレードの最大損失**：資金の5〜7%（約1.5〜2万円）に固定。損切り幅から逆算して株数を決める。
- **損切り**：機械的に実行。「戻るかも」で持ち続けない。
- **利確**：段階的に。+50〜100%で半分利確して原資回収、残りを伸ばす。
- **撤退ライン**：資金が15万円（半分）まで減ったら一旦すべて手仕舞い、手法を見直す。
- **資金性質**：失っても生活に支障のない余剰資金であること。
- **信用取引**：最初は現物で優位性を確認してから。いきなりフルレバは退場が先に来る。

---

## 8. 検証時の心構え

- バックテストは「儲かる証拠探し」ではなく **「どの相場で壊れるか探し」** のつもりで行う。
- **過剰最適化（カーブフィッティング）が最大の罠**。短期間データで効いただけの偶然を優位性と誤認しない（10年分・複数の相場局面で検証する理由）。
- 手数料・スリッページを必ずコストに含めて検証する。

---

## 9. 進め方（フェーズ）

1. **接続確認**：J-Quants Free でデータ取得の最小コードを動かす（自宅PC・0円）。
2. **ロジック明文化**：デイトレの売買ルールを検証可能な形で厳密に定義する。
3. **共通ロジック層の実装**：指標計算＋売買判定をエンジン非依存の純Python関数として書く。
4. **二重バックテスト**：Backtrader と NautilusTrader それぞれに共通ロジックを接続し、手数料・スリッページ込みで検証 → 両者の結果を突き合わせる。本格検証時のみ Standard＋分足を契約し、終了後に解約。
5. **フォワードテスト**：ペーパートレード（模擬取引）で数週間〜数ヶ月、実環境の挙動を確認。
6. **少額の実資金**：優位性が確認できてから小さく開始。
7. **段階的に拡大**：勝てているときだけロットを上げる。本番常時稼働になったら国内VPSへ移行。

---

## 10. 次のアクション

- [ ] J-Quants に Free 登録（接続確認の準備）
- [ ] 証券口座（三菱UFJ eスマート証券）開設・信用口座申込（開設に日数がかかるため早めに）
- [x] J-Quants からデータ取得する最小コードの作成（`scripts/fetch_jquants_minimal.py`）

---

## 11. リポジトリ構成と動かし方

### ディレクトリ

```
daytrade/
├── README.md                # 本ドキュメント
├── pyproject.toml           # パッケージ設定 / pytest 設定
├── requirements.txt         # 依存（重い依存はコメントで段階導入）
├── .env.example             # J-Quants 認証情報のひな型（.env にコピーして使う）
├── scripts/
│   └── fetch_jquants_minimal.py   # フェーズ1：J-Quants 接続確認の最小コード
├── src/daytrade/
│   ├── core/                # 共通ロジック層（エンジン非依存・単一の真実）
│   │   ├── types.py         #   Action / Features / PositionState / StrategyParams
│   │   ├── indicators.py    #   指標計算（SMA / ATR / VWAP / 出来高平均、日次リセット）
│   │   └── signals.py       #   売買判定 decide() ＝ 単一の真実 ＋ 基準実装 generate_signals()
│   ├── data/
│   │   └── jquants.py       # J-Quants API クライアント（過去検証専用）
│   └── adapters/            # 各エンジンへの接続部分（判定は core に委譲）
│       ├── backtrader_adapter.py    # Backtrader 用 Strategy ファクトリ
│       └── nautilus_adapter.py      # NautilusTrader 用（骨格）
└── tests/                   # core 層のユニットテスト（pytest）
```

設計の核（README 5章）：売買判定は `core/signals.py` の `decide()` に集約し、
Backtrader / NautilusTrader アダプタはそれを呼ぶだけにする。
ロジック修正は1箇所で済み、両エンジンの差分が「エンジンの差」だけに絞れる。

### セットアップ

```bash
python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env        # JQUANTS_EMAIL / JQUANTS_PASSWORD を記入
```

### フェーズ1：J-Quants 接続確認

```bash
PYTHONPATH=src python scripts/fetch_jquants_minimal.py 7203
PYTHONPATH=src python scripts/fetch_jquants_minimal.py 7203 --from 2024-01-01 --to 2024-03-31
```

Free プランはデータが12週間遅延するため、取得できる最新日付が数ヶ月前になるのは正常。

### テスト

```bash
pytest          # 共通ロジック層（指標・売買判定）のユニットテスト
```

### 段階導入のメモ

- 指標：いまは pandas/numpy 実装。`requirements.txt` の `pandas-ta` / `TA-Lib` を有効化すれば差し替え可能（入出力の形は不変）。
- エンジン：`backtrader` / `nautilus_trader` は重い依存のためコメントアウト中。フェーズ4で有効化する。
