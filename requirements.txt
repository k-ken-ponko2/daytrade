# --- 土台（必須） ---
pandas>=2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# --- データキャッシュ（任意） ---
pyarrow>=14.0   # parquet キャッシュ用。無ければ DataStore は自動で CSV にフォールバック

# --- 可視化 ---
matplotlib>=3.7
# plotly>=5.18  # 必要になったら有効化

# --- 指標計算 ---
# pandas-ta>=0.3.14b0   # 手軽。導入したら core/indicators.py を差し替え可
# TA-Lib>=0.4.28        # 高速・網羅。別途Cライブラリのインストールが必要

# --- バックテストエンジン（二重/三重検証で導入） ---
# backtrader>=1.9.78.123    # 二重検証・デモで使用（pip install backtrader）
# nautilus_trader>=1.190.0  # NautilusTrader アダプタ用（重い依存。導入時のみ）

# --- 開発 ---
pytest>=7.4
